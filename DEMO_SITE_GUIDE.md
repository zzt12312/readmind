# ReadMind 演示站使用说明

## 1. 访问方式

当前演示站通过服务器公网 IP 访问：

`http://43.139.112.45:3000`

说明：

- 当前没有绑定域名
- 当前没有配置 HTTPS
- 当前使用公网 IP + 端口方式提供演示
- 如果外网暂时打不开，请先检查云服务器安全组是否已经放行 `TCP 3000`

## 2. 演示站是什么

这个演示站是 ReadMind 的公开演示版本，主要用于：

- 展示书库浏览效果
- 展示笔记工作台
- 展示单本书问答体验
- 展示知识图谱
- 展示复习中心

演示站使用的是**预置缓存数据**，不是实时扫描你本地 Obsidian 目录。

## 3. 当前演示模式说明

为了保证公开访问时更稳定、也避免持续消耗模型费用，演示站当前采用的是：

- 静态缓存数据
- 本地检索
- 预生成结果
- 本地回退回答

也就是说：

- 书库、笔记、图谱、复习内容都可以正常浏览
- 智能问答仍然可以体验
- AI 洞察和书籍摘要仍然可以体验
- 但这些能力默认**不会调用外部 DeepSeek 模型**

## 4. 推荐体验路径

如果是第一次使用，建议按这个顺序体验：

1. 首页  
   查看书籍总数、笔记总数、分类数、待复习数量。

2. 书库  
   浏览书籍封面，点击进入某本书查看摘要和高亮。

3. 笔记工作台  
   当前默认会进入《南明史》的笔记内容，方便直接体验真实结果。

4. 智能问答  
   建议直接提问：
   - `《南明史》里最值得回看的 3 个观点是什么？`
   - `《南明史》里有哪些值得复习的判断？`

5. 知识图谱  
   查看主题聚类和关系图。

6. 复习中心  
   体验复习卡片和评分流程。

## 5. 各页面说明

### 5.1 首页

首页展示的是整个演示书库的概览信息，包括：

- 书籍数
- 笔记数
- 分类数
- 待复习数量
- 最近整理的书

### 5.2 导入中心

导入中心在演示站里是**模拟交互模式**。

你可以看到：

- 演示数据已经就绪
- 刷新演示数据
- 模拟上传

这里的目的主要是让用户体验页面流程，不会真正导入服务器本地文件。

### 5.3 书库

书库页支持：

- 浏览书籍
- 搜索书名、作者、标签
- 进入书籍详情页

### 5.4 笔记工作台

笔记工作台默认展示《南明史》，这样用户一进来就能看到：

- 左侧筛选
- 中间笔记列表
- 右侧 AI 洞察

### 5.5 智能问答

问答页支持：

- 单本书提问
- 连续追问
- 引用来源展示

但请注意：当前演示模式下，回答默认来自：

- 本地检索结果
- 预生成内容
- 本地回退逻辑

不会消耗外部模型 token。

### 5.6 知识图谱

图谱页支持：

- 领域聚类
- 知识主题视角
- 主题关系查看

### 5.7 复习中心

复习页支持：

- 查看待复习卡片
- 显示答案
- 标记掌握程度

## 6. 当前已知局限

当前演示站有这些限制：

1. 数据不是实时同步的  
演示站使用的是预置缓存数据，不会实时读取你本地 Obsidian 文件。

2. 问答不是实时大模型生成  
为了控制成本并保证公开访问稳定性，当前默认不走 DeepSeek，而是展示本地回退或预生成结果。

3. 导入功能是演示态  
导入中心可以完整体验交互，但不会真正把用户上传的文件永久写入演示站数据集。

4. 没有用户隔离  
当前演示站不是多用户正式产品，不适合承载真实个人数据上传和长期保存。

5. 没有 HTTPS  
当前使用的是 IP + 端口方式，没有域名，也没有证书。

6. 没有备案域名  
当前没有购买域名，也没有备案，所以暂时不提供正式域名访问方式。

7. 页面内容以展示为主  
演示站的目标是“完整体验核心功能”，不是生产可用服务。

## 7. 适合如何对外说明

你可以这样向别人介绍：

> 这是 ReadMind 的公开演示站。为了保证体验稳定、避免消耗真实模型费用，演示站使用的是预置阅读缓存和本地回退回答，但可以完整展示书库、笔记、问答、图谱和复习等核心功能。

## 8. 如果页面看起来不对

如果你更新过演示站版本，而浏览器页面没有变化，可以先尝试：

1. 强制刷新页面  
   Mac: `Cmd + Shift + R`

2. 清除浏览器缓存后重新访问

3. 使用无痕窗口打开

## 9. 当前最适合演示的页面

如果你要给老师、同学或面试官演示，最推荐：

1. 首页
2. 书库
3. 《南明史》笔记工作台
4. 智能问答
5. 知识图谱
